apiVersion: v1
kind: Service
metadata:
  name: kafka-server
  namespace: springbit
spec:
  selector:
    app: kafka-server
  ports:
    - protocol: TCP
      port: 9092
      targetPort: 9092
  type: ClusterIP
